
int soilProfile =-1;

void setup(){
 
  fullScreen();  //make the UI fullscreen
  background(BLACK); //make the background black
  frameRate(30); //cap the frame rate to 30 frames per second
  
  redraw();
  startLogic();
}


int[] startLogic(){
  //TODO //first generation of plant with random variables
  return new int[2];
}